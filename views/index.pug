extends layout

block content
  h1= title
  p Welcome to #{title}

  h1 Dynamic content
  if error
    p An unexpected error occurred while fetching content! 
  else 
    p The sharing platform currently has the following records:
    
    ul
      li #[strong Books:] !{data.book_count} books
      li #[strong Copies:] !{data.book_instance_count} copies
      li #[strong Currently Available Copies:] !{data.book_instance_available_count} available copies
      li #[strong Publishers:] !{data.author_count} main publishers
      li #[strong Subjects:] !{data.subject_count} related subjects
