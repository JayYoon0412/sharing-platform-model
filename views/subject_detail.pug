extends layout 

block content 


    div
        h4 Books

        dl 
            each book in subject_books
                dt 
                    a(href=book.url) !{book.title}
                dd 
                    p !{book.summary}
            else
                p No books categorized as '!{subject.name}'
