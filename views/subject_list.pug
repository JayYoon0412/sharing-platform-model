extends layout 

block content 
    h1=title 

    ul 
        each subject in subjects_list
            li 
                a(href=subject.url) !{subject.name}
        else 
            li There are currently no books in storage.